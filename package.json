{
  "name": "home-budget-agent",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "prepare": "husky",
    "generate:types": "cd backend && uv run python scripts/export_openapi.py && cd ../frontend && pnpm generate:types",
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:backend": "cd backend && DATABASE_URL=sqlite:///../data/database.db uv run uvicorn app.main:app --reload --port 8001"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "bash -c 'cd frontend && npx eslint --no-warn-ignored --max-warnings=0 \"${@}\"' --"
    ],
    "backend/**/*.py": [
      "bash -c 'cd backend && uv run ruff check \"${@}\"' --"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild"
    ]
  }
}

